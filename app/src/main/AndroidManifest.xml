<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logofit3"
        android:label="Body Transformation"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".dayone.WorkOutOne0" />
        <activity android:name=".MainPageActivity" />
        <activity android:name=".frontpagesapp.ActivityGirls" />
        <activity android:name=".frontpagesapp.ActivityBoys" />
        <activity android:name=".frontpagesapp.NextPage" />
        <activity android:name=".dayone.DayOneActivityList" />
        <activity android:name=".frontpagesapp.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>